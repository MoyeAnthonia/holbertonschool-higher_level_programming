#!/usr/bin/node
const firstArg = parseInt(process.argv[2]);
const secondArg = parseInt(process.argv[3]);
const result = any;

function add(a, b) {
    if (isNaN(firstArg, secondArg )) {
        return NaN;
    } else {
    console.log( a + b)
    }

}

console.log(add(firstArg, secondArg))
